/**
 * CariPropShop Chat - Frontend JavaScript
 * Note: Most chat functionality is inline in the widget template.
 * This file can be used for additional enhancements if needed.
 */

(function($) {
	'use strict';

	$(document).ready(function() {
		// Optional: Sound notification for new messages
		if (cpsChatData.options.enable_sound) {
			// Sound handling can be added here if needed
		}
	});

})(jQuery);